server {
  listen 80 default_server;
  listen [::]:80 default_server;
   root /var/www/html;
   
   
   location /example {
    try_files $uri/example/web_release
    index index.html index.htm;
  }
  location /payroll{
  try_files $uri/example/payroll_dashboard
    index index.html index.htm;
  }
 
  location /rest{
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1; # Important, do not omit
  }
}
