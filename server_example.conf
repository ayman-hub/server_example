server {
  listen 80 default_server;
  listen [::]:80 default_server;
  root  /var/www/html/payroll_dashboard;
   
   
   location /{
    try_files $uri $uri/ /index.html;
  }
  
  location /rest{
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1; # Important, do not omit
  }
}
